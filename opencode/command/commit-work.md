---
description: Generate comprehensive git commits
---

Help the user create high-quality, production-ready git commits that are easy to review, safe to ship, and follow best practices.

First, invoke the skill tool to load the commit-work skill:

```
skill({ name: 'commit-work' })
```

Then follow the skill instructions to help the user with their git commit.

<user-request>
$ARGUMENTS
</user-request>
