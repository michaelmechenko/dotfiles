#!/bin/sh
# Keep origin local to this Extrakto invocation. The vendored launcher carries it
# through popup/split -e, so concurrent clients cannot overwrite shared state.
EXTRAKTO_ORIGIN_PANE="$1" exec "$HOME/.config/tmux_plugins/extrakto/scripts/open.sh" "$1"
